// console.log("Hello world");

// // In Node.js, `global` is the global object
// console.log(global); 

// // `this` refers to an empty object in the module scope of Node.js (not the global object)
// console.log(this); 

// // `globalThis` refers to the global object in any environment (both Node.js and browsers)
// console.log(globalThis);

//Both `global` and `globalThis` point to the global object in Node.js
// console.log(global === globalThis); // true
