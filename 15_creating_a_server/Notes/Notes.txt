#Steps:
1.create a repository
2.Initialize the repository
3.node_modules, package.json, package-lock.json
4.Install express
5.create a server
6.Listen to port
7.write request handlers for /test, /hello
8.Install nodemon and update scripts inside package.json

#Read about :
- diff between caret and tilde (^ vs ~)
- what are dependencies
- what is the use of "-g" while npm Install

#Notes :
1.Listen is accepting the req from outside worlds, once we are creating server.
2.From client side we make request (i.e, localhost:3000) and from server side "listen" is used to accept req from client side and we send response from server side.

